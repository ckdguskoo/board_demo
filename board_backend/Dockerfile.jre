# JAR 파일 실행용 경량 Dockerfile
# 이 파일은 JAR 파일이 이미 빌드되어 있을 때 사용합니다
FROM eclipse-temurin:21-jre-alpine

WORKDIR /app

# JAR 파일은 볼륨 마운트로 제공됩니다
# docker-compose.yml에서 볼륨 마운트 설정 필요

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "board-demo.jar"]

